server:
  port: 8080
  servlet:
    context-path: /book
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book_service
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true